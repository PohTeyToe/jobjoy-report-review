# Public — shipped to the browser. Safe (anon key is public-by-design; RLS enforces access).
PUBLIC_SUPABASE_URL=https://kuvojgdateaosaeiqbbb.supabase.co
PUBLIC_SUPABASE_ANON_KEY=replace-me

# URL-secret gate for /admin/<secret>. Rotate if the link ever leaks.
PUBLIC_ADMIN_SECRET=replace-with-32-char-random-string
